{
  "name": "trstlyr-protocol",
  "version": "0.0.1",
  "private": true,
  "description": "The trust layer for the agent internet",
  "license": "Apache-2.0",
  "packageManager": "pnpm@9.0.0",
  "engines": {
    "node": ">=20.0.0"
  },
  "scripts": {
    "build": "pnpm -r build",
    "dev": "pnpm --filter @trstlyr/api dev",
    "test": "pnpm -r --if-present test",
    "lint": "eslint \"packages/*/src/**/*.ts\" \"apps/*/src/**/*.ts\"",
    "typecheck": "pnpm -r typecheck",
    "deploy:schema": "pnpm --filter @trstlyr/contracts deploy:schema",
    "format": "prettier --write \"packages/*/src/**/*.ts\" \"apps/*/src/**/*.ts\"",
    "format:check": "prettier --check \"packages/*/src/**/*.ts\" \"apps/*/src/**/*.ts\""
  },
  "workspaces": [
    "packages/*",
    "contracts"
  ],
  "devDependencies": {
    "@typescript-eslint/eslint-plugin": "^8.56.1",
    "@typescript-eslint/parser": "^8.56.1",
    "eslint": "^10.0.2",
    "eslint-config-prettier": "^10.1.8",
    "prettier": "^3.8.1"
  },
  "dependencies": {
    "sharp": "^0.34.5"
  }
}
